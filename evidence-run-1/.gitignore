# Run outputs are regenerated by drive-session.js / probe-autotrade-persistence.js.
# They are NOT an evidence bundle and must never be committed or presented as one --
# see EVIDENCE-RUN-1.md §9. Only the report and the reproducible drivers are tracked.
*.db
*.db-shm
*.db-wal
session-1-log.json
probe-autotrade-result.json
upbit-ticker-probe.json
.run_ts
