"use strict";const {canonicalHash}=require("./canonical-hash.js");const {REQUIRED}=require("./risk-safety-drill-runner.js");function verifyRiskSafetyDrills(result){const errors=[];if(!result||result.drills?.length!==REQUIRED.length)errors.push("drill coverage mismatch");for(const d of result?.drills||[]){const raw={drillId:d.drillId,expectedDecision:d.expectedDecision,actualDecision:d.actualDecision,reasonCodes:d.reasonCodes,orderCount:d.orderCount,fillCount:d.fillCount,cashMutation:d.cashMutation,positionMutation:d.positionMutation,stateTransition:d.stateTransition,alert:d.alert,persistenceResult:d.persistenceResult,reconciliationResult:d.reconciliationResult,result:d.result};if(d.sha256!==canonicalHash(raw))errors.push(`hash mismatch:${d.drillId}`);if(d.result==="PASS"&&d.expectedDecision!==d.actualDecision)errors.push(`decision mismatch:${d.drillId}`);if(d.actualDecision!=="ALLOW"&&(d.orderCount!==0||d.fillCount!==0||d.cashMutation!==0||d.positionMutation!==0))errors.push(`mutation after block:${d.drillId}`);}return {status:errors.length?"FAIL":"PASS",errors:Object.freeze(errors)};}module.exports={verifyRiskSafetyDrills};
