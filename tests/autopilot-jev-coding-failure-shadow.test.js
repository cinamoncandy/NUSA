const test=require("node:test"); const assert=require("node:assert/strict");
const {observeJevCodingFailureShadow}=require("../dist/apps/autopilot/src/jevCodingFailureShadow.js");
const req={headSha:"a".repeat(40),workflowRunId:42,executionId:"e:1",dedupeKey:"d:1",reason:"ci-failure:42"};
test("deterministic pre-filter skips already-classified failures",async()=>{for(const failureClass of ["transient","validation_failure","permission_auth","infrastructure","executor_unavailable","unsafe_ambiguous",null]) assert.equal(await observeJevCodingFailureShadow({runnerRequest:req,failureReason:"x",failureClass,env:{NUSA_JEV_SHADOW_ENABLED:"true"}}),null);});
test("eligible unknown deterministic failure emits typed shadow fallback without provider",async()=>{const r=await observeJevCodingFailureShadow({runnerRequest:req,failureReason:"UNCLASSIFIED_FAILURE",failureClass:"deterministic",env:{NUSA_JEV_SHADOW_ENABLED:"true"}});assert.equal(r.decisionType,"WORKFLOW_FAILURE_CLASSIFICATION");assert.equal(r.selectedOutcome,"INSUFFICIENT_EVIDENCE");assert.equal(r.usableForRouting,false);assert.equal(r.aiAuthority,"ZERO_AUTHORITY");assert.equal(r.productionMutationAllowed,false);assert.equal(r.liveAuthority,"NONE");});
test("disabled Jev preserves existing path and emits non-routing evidence",async()=>{const r=await observeJevCodingFailureShadow({runnerRequest:req,failureReason:"UNCLASSIFIED_FAILURE",failureClass:"deterministic",env:{}});assert.equal(r.selectedOutcome,"INSUFFICIENT_EVIDENCE");assert.equal(r.reasonCode,"SHADOW_FALLBACK");assert.equal(r.usableForRouting,false);});
