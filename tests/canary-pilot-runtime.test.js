const test=require("node:test"),assert=require("node:assert/strict"),{CanaryPilotRuntime,createCanaryApproval,verifyCanaryEvents}=require("../dist/apps/desktop/src/canaryPilotRuntime.js"),{PaperBroker}=require("../dist/apps/desktop/src/paperBroker.js"),{ControlPlane}=require("../dist/apps/desktop/src/controlPlane.js"),{RuntimeCommandService}=require("../dist/apps/desktop/src/runtimeCommandService.js"),{StrategyEngine,SmaCrossoverStrategy}=require("../dist/apps/desktop/src/strategyEngine.js");
const f={strategy:"s",config:"c",runtime:"r",riskPolicy:"p"};const approval=createCanaryApproval({approvalId:"a",approvedBy:"owner",approvedAt:1,expiresAt:10000,mode:"PAPER_CANARY",sourceCommitSha:"commit",allowedSymbol:"KRW-BTC",allowedStrategyId:"sma",maxDurationMinutes:1,maxOrders:1,maxTrades:1,maxOpenPositions:1,maxOrderQuantity:1,maxOrderNotional:100,maxGrossExposure:100,maxDailyLoss:1,maxSessionLoss:1,maxDrawdown:1,maxConsecutiveLosses:1,fingerprints:f});function pilot(){const broker=new PaperBroker(1000,"KRW-BTC",0),control=new ControlPlane("sma"),runtime=new RuntimeCommandService(broker,control,new StrategyEngine(new SmaCrossoverStrategy(2,3)),{save(){}},{evaluate(){return {status:"ALLOW",reasonCodes:[]}}});return {broker,p:new CanaryPilotRuntime({sessionId:"c",symbol:"KRW-BTC",strategyId:"sma",sourceCommitSha:"commit",fingerprints:f,approval,runtime,broker})};}const check={paperOnly:true,shadowEvidencePassed:true,deploymentIntegrity:true,reconciliation:true,killSwitch:false,openP0:false,marketDataHealthy:true,warmedUp:true,automaticTrading:false,persistenceHealthy:true};
test("Canary requires explicit approval/precheck and routes bounded orders through RuntimeCommandService",()=>{const {broker,p}=pilot();assert.equal(p.precheck(check,2),"READY");p.start(3);p.order({commandId:"one",side:"BUY",quantity:.5,price:100,now:4});assert.equal(broker.snapshot(100).orders.length,1);assert.deepEqual(verifyCanaryEvents(p.eventLog()),[]);p.order({commandId:"two",side:"BUY",quantity:.5,price:100,now:5});assert.equal(broker.snapshot(100).orders.length,1);assert.equal(p.snapshot().status,"LIMIT_REACHED");});
test("Canary blocks invalid precheck and restart never resumes",()=>{const {broker,p}=pilot();assert.equal(p.precheck({...check,killSwitch:true},2),"HALTED");assert.equal(broker.snapshot(100).orders.length,0);const x=pilot().p;x.precheck(check,2);x.start(3);x.restart();assert.equal(x.snapshot().status,"INVALIDATED");});
