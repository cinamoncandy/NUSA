const test = require("node:test"); const assert = require("node:assert/strict"); const fs = require("node:fs"); const os = require("node:os"); const path = require("node:path"); const { spawnSync } = require("node:child_process"); const { canonicalHash } = require("../scripts/lib/canonical-hash.js"); const { REQUIRED_EVIDENCE_TYPES, createStrategyResearchScorecard } = require("../scripts/lib/strategy-research-scorecard.js"); const { verifyStrategyResearchScorecard } = require("../scripts/lib/strategy-research-scorecard-verifier.js");
const identity = { strategyId: "sma", strategyVersion: "1", datasetId: "set", datasetVersion: "1", executionId: "run", riskProfileId: "risk" };
function request() { return { scorecardId: "card", identity, evaluatedAtMs: 1, evidence: REQUIRED_EVIDENCE_TYPES.map((type) => { const payload = { sampleSufficiency: "PASS", benchmarkCompetitiveness: "PASS" }; return { evidenceId: type, type, source: "REAL", assessment: "PASS", identity, payload, payloadSha256: canonicalHash(payload) }; }) }; }
test("sealed real evidence creates a deterministic zero-authority scorecard", () => { const input = request(); const card = createStrategyResearchScorecard(input); assert.equal(card.decision, "PROMOTE_TO_EXTENDED_PAPER_REVIEW"); assert.equal(card.productionMutationAllowed, false); assert.equal(card.promotionAuthority, false); assert.equal(verifyStrategyResearchScorecard(input, card).status, "PASS"); assert.deepEqual(card, createStrategyResearchScorecard({ ...input, evidence: [...input.evidence].reverse() })); });
test("missing, unverified, synthetic, tampered, and mismatched evidence fail closed", () => { const missing = request(); missing.evidence.pop(); assert.equal(createStrategyResearchScorecard(missing).decision, "INSUFFICIENT_EVIDENCE"); const unverified = request(); delete unverified.evidence[0].payloadSha256; assert.equal(createStrategyResearchScorecard(unverified).decision, "INSUFFICIENT_EVIDENCE"); const synthetic = request(); synthetic.evidence.forEach((item) => { item.source = "SYNTHETIC"; }); assert.equal(createStrategyResearchScorecard(synthetic).decision, "CONTINUE_RESEARCH"); const tampered = request(); tampered.evidence[0].payload = {}; assert.equal(createStrategyResearchScorecard(tampered).decision, "INVALID"); const mismatched = request(); mismatched.evidence[0].identity = { ...identity, executionId: "other" }; assert.equal(createStrategyResearchScorecard(mismatched).decision, "INVALID"); });
test("explicit research failures drive revise, reject, and hold decisions", () => { const revise = request(); revise.evidence.find((item) => item.type === "REGIME_ANALYSIS").assessment = "FAIL"; assert.equal(createStrategyResearchScorecard(revise).decision, "REVISE_STRATEGY"); const reject = request(); reject.evidence.find((item) => item.type === "PAPER_OPERATIONAL_SAFETY").assessment = "FAIL"; assert.equal(createStrategyResearchScorecard(reject).decision, "REJECT_STRATEGY"); const hold = request(); hold.evidence.find((item) => item.type === "WALK_FORWARD").assessment = "INCONCLUSIVE"; assert.equal(createStrategyResearchScorecard(hold).decision, "HOLD"); });
test("CLI emits a verifiable artifact once and refuses overwrite", () => { const directory = fs.mkdtempSync(path.join(os.tmpdir(), "scorecard-")); const input = path.join(directory, "request.json"); const output = path.join(directory, "result.json"); fs.writeFileSync(input, JSON.stringify(request())); const first = spawnSync(process.execPath, ["scripts/run-strategy-research-scorecard.js", "--request", input, "--output", output], { encoding: "utf8" }); assert.equal(first.status, 0, first.stderr); assert.equal(JSON.parse(fs.readFileSync(output, "utf8")).verification.status, "PASS"); const second = spawnSync(process.execPath, ["scripts/run-strategy-research-scorecard.js", "--request", input, "--output", output], { encoding: "utf8" }); assert.equal(second.status, 1); fs.rmSync(directory, { recursive: true, force: true }); });
