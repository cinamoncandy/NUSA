export enum ProductionAdapterAttestationDecision { ATTESTED="ATTESTED", FAILED="FAILED" }
export interface ProductionAdapterInventory { readonly productionExchangeAdapter:boolean; readonly liveTradingExecutor:boolean; readonly realOrderSubmitter:boolean; readonly capitalMovementAdapter:boolean; }
export interface ProductionAdapterAttestation { readonly attestationId:string; readonly decision:ProductionAdapterAttestationDecision; readonly productionMutationAllowed:false; readonly blockers:readonly string[]; readonly evaluatedAtMs:number; }
export function attestProductionAdapterAbsence(input:{readonly attestationId:string;readonly inventory:ProductionAdapterInventory;readonly nowMs:number;}):ProductionAdapterAttestation { if(!input.attestationId.trim()||!Number.isSafeInteger(input.nowMs)||input.nowMs<0)throw new Error("valid attestation identity and time are required");const b:string[]=[];if(input.inventory.productionExchangeAdapter)b.push("PRODUCTION_EXCHANGE_ADAPTER_PRESENT");if(input.inventory.liveTradingExecutor)b.push("LIVE_TRADING_EXECUTOR_PRESENT");if(input.inventory.realOrderSubmitter)b.push("REAL_ORDER_SUBMITTER_PRESENT");if(input.inventory.capitalMovementAdapter)b.push("CAPITAL_MOVEMENT_ADAPTER_PRESENT");return Object.freeze({attestationId:input.attestationId,decision:b.length?ProductionAdapterAttestationDecision.FAILED:ProductionAdapterAttestationDecision.ATTESTED,productionMutationAllowed:false,blockers:Object.freeze(b.sort()),evaluatedAtMs:input.nowMs}); }
