# NUSA Investment Performance Engine Versioning

Benchmark and promotion policies must be versioned independently from strategy code. A change to eligibility thresholds, ranking weights, cost assumptions, or coverage requirements changes research semantics and therefore must create new evidence rather than silently reinterpret old results.

Historical scorecards remain immutable evidence tied to their policy version, dataset identity, source SHA, and generated timestamp.
