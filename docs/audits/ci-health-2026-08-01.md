# NUSA CI Health Audit

Audit date: 2026-08-01
Audited main SHA: `10ad991ee9cded7f7784064641dd87dbc79c1c69`
Recovery head: `1be2482c2f4bffe809790fc613bd088dcea94af5`

## Workflow

The repository has `.github/workflows/ci.yml`. It runs on `push` to `main` and
`agent/**`, on pull requests targeting those branches, and manually.

The job uses Windows, Node 24, pnpm 11.7.0, and a frozen lockfile. It runs:

- preflight through the test entrypoint
- typecheck
- build
- lint
- UI tests
- deterministic Playwright Chromium install and E2E
- package validation
- release-readiness check
- focused safety/readiness suites
- full isolated tests
- coverage baseline and artifact upload

## Evidence

- main push run `30684423092`: completed / success
- main check run `test`: completed / success
- PR #44 run `30687933986`: completed / success
- PR #44 run `30687935055`: completed / success

No failed current-head check is hidden. The only annotation is the GitHub
Actions Node.js 20 deprecation warning for action versions currently forced onto
Node 24; it is not a job failure.

## Governance finding

`GET /repos/cinamoncandy/NUSA/branches/main/protection` returns HTTP 404
(`Branch not protected`). Therefore required checks are visible and passing but
are not enforced by branch protection. This is a HIGH/P1 repository-governance
finding and requires owner repository-settings action.
