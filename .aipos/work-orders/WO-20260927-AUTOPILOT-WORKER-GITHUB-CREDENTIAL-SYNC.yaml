id: WO-20260927-AUTOPILOT-WORKER-GITHUB-CREDENTIAL-SYNC
title: "Synchronize the Worker GitHub API credential through the canonical deploy path"
status: IMPLEMENTED_PENDING_EXACT_HEAD
owner: nusa-development
repository: cinamoncandy/NUSA
base_branch: main
base_commit: f35b0ad935ca469a283cae1e4658075ca9cd82a7
implementation_branch: codex/autopilot-worker-token-sync
implementation_head: 17288b2c6a25a12f696e0efa183d84cf0469f663
pull_request: 2325
authority_impact: none
objective: "Make the existing Cloudflare Worker GitHub API credential lifecycle converge with the canonical deployment workflow so exact-head verification and bounded publication can proceed without a detached secret path."
canonical_owner: "Existing autopilot Cloudflare deployment workflow and Worker coding runner"
scope:
  include:
    - "Validate the repository secret NUSA_AUTOPILOT_GITHUB_TOKEN without printing its value."
    - "Synchronize it as the Worker secret NUSA_GITHUB_TOKEN before exact revision deployment."
    - "Preserve existing exact-head, fail-closed, sandbox, audit, release, and zero-authority gates."
    - "Add regression assertions for the preflight mapping and secret synchronization contract."
  exclude:
    - "New credential provider, broad PAT, token logging, or local-token copying."
    - "LIVE, broker, order, production asset, or AI authority changes."
acceptance:
  - "Missing or short repository credential fails closed before Cloudflare mutation."
  - "Configured repository credential is mapped to NUSA_GITHUB_TOKEN without appearing in logs or artifacts."
  - "PAPER_ONLY, liveAuthority=NONE, productionMutationAllowed=false, aiAuthority=ZERO_AUTHORITY remain unchanged."
verification_commands:
  - "node --test tests/autopilot-cloudflare-deploy.test.js"
  - "pnpm run validate"
  - "pnpm run typecheck"
  - "git diff --check"
evidence:
  implementation_status: IMPLEMENTED_PENDING_EXACT_HEAD
  local_validation: "Focused workflow contract, validate, typecheck, and diff check PASS."
  external_validation: "PR #2325 exact-head required workflows PASS, including Cloudflare credential preflight; Audit/Release must bind this exact head."
safety:
  paper_only: true
  liveAuthority: NONE
  productionMutationAllowed: false
  aiAuthority: ZERO_AUTHORITY
  fail_closed: true
next_permitted_action: "Run canonical Audit/Release for PR #2325; after merge, verify exact Worker deployment and run one bounded dogfood execution."
