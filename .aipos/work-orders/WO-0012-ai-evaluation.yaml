id: WO-0012
schema_version: 1
title: "Implement deterministic AI Evaluation"
status: BLOCKED
priority: 85
objective: "Evaluate AI research outputs for accuracy, replay determinism, provenance, safety, and regression stability without granting execution authority."
scope:
  - "Replay identical Backtest and Strategy Optimizer inputs and detect divergence."
  - "Benchmark labeled recommendations for accuracy, outcome success, coverage, and calibration."
  - "Compare replay results with immutable regression baselines."
  - "Emit an immutable evaluation report with explicit safety gates."
exclusions:
  - "No new model training or external model calls."
  - "No live trading or production mutation."
  - "No automatic strategy promotion or replacement."
safety:
  productionMutationAllowed: false
  liveTrading: disabled
  credentials: not-configured
verification:
  - "AI Evaluation focused tests"
  - "Strategy, Backtest, Optimizer regression tests"
  - "pnpm run typecheck"
  - "pnpm run lint"
  - "pnpm run build"
  - "git diff --check"
completion_criteria:
  - "Replay divergence fails the evaluation gate."
  - "Dataset provenance is exact for every benchmark case and replay."
  - "Regression baseline mismatch is reported as FAIL."
  - "Unsafe or incomplete research output cannot produce PASS."
  - "All verification commands pass and CI is green."
blockers:
  - "PR #100 CI security gate cannot obtain pnpm audit JSON; local and GitHub runner report fetch failed. The gate remains fail-closed."
current_status: "Implemented in commit 9a6e5ba; local focused/regression verification passed; PR #100 Mobile Native passed, but CI security gate failed closed because pnpm audit returned fetch failed."
next_action: "Restore a working dependency advisory audit endpoint or approved CI audit service, then rerun PR #100 without weakening the security gate."
rollback: "Revert only the focused evaluation commit; no trading state or production data is changed."
