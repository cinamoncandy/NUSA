id: WO-20260923-AUTOPILOT-EXECUTE-PROVIDER-GATE
title: "Gate the Worker coding entry on the provider capacity wait"
status: MERGED
owner: nusa-development
repository: cinamoncandy/NUSA
base_branch: main
base_commit: d612756c6eacf861cc26bfb037ec4bbca057e262
implementation_branch: claude/nusa-repository-j93sd8-execute-gate
objective: "No execution of any identity may spend a coding-provider call while the provider is inside its recorded retry window."
parent_work_order: WO-20260923-AUTOPILOT-PROVIDER-WAIT-ACROSS-MAIN
authority_impact: none
finding: "#2234 made the scheduler honour the provider-keyed wait, but repository_dispatch executions (for example CI-repair) enter the Worker through /coding/execute, which only checked the per-execution lease. A new identity inside the provider window therefore still called the provider."
scope:
  include:
    - "The coding runner re-reads the provider wait immediately before every Workers AI call (after lease and GitHub verification)"
    - "Inside the window: BLOCKED_RATE_LIMIT with reason WAITING_PROVIDER_CAPACITY and the provider's nextRetryAt; the existing stop path records the per-execution WAIT and telemetry"
    - "Unreadable provider state: EXECUTION_FAILED PROVIDER_CAPACITY_STATE_UNAVAILABLE with no provider call (fail closed)"
    - "Terminal per-execution dedupe (COMPLETED/DISPATCHED) is still decided by the lease path first"
  exclude:
    - "Serializing provider calls already in flight when a stop lands (would remove parallelism); the residual window is the single call between the check and AI.run"
    - "Lease, dedupe, execution identity and per-execution WAITING semantics"
    - "LIVE, production mutation, credentials, new queue/scheduler/agent"
acceptance:
  - "A different execution on a different main makes no provider call inside the provider wait."
  - "A wait recorded after admission or between repair attempts stops the next provider call."
  - "Replays of COMPLETED/DISPATCHED identities stay DUPLICATE_EXECUTION_SUPPRESSED."
  - "Suppressed executions are recorded in execution telemetry and their own WAITING record."
  - "An unreadable provider wait fails closed before any provider call."
  - "PAPER_ONLY, liveAuthority=NONE, productionMutationAllowed=false, aiAuthority=ZERO_AUTHORITY remain unchanged."
verification_commands:
  - "npx tsx --test apps/autopilot/src/*.test.ts"
  - "pnpm run typecheck"
  - "pnpm run lint"
evidence:
  implementation_status: MERGED_PR_2237_f72f6b0c
  mutation_verification:
    - "execute ignores the provider wait -> 1 failure"
    - "execute fails open on an unreadable wait -> 1 failure"
  external_validation: "COMPLETED: exact-head protected CI passed on d60d8276; independent Audit and Release gate passed; merged by nusa-release-authority[bot] as PR #2237 merge f72f6b0c; merged blobs verified byte-identical to the validated head."
safety:
  paper_only: true
  liveAuthority: NONE
  productionMutationAllowed: false
  aiAuthority: ZERO_AUTHORITY
  fail_closed: true
next_permitted_action: "Completed (PR #2237, merge f72f6b0c). No further action on this work order."
