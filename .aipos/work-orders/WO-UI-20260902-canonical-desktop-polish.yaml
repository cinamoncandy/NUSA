id: WO-UI-20260902-CANONICAL-DESKTOP-POLISH
schema_version: 1
title: "Canonical desktop trading UX polish"
status: BLOCKED_ENVIRONMENT
authority_impact: none
owner: owner-requested
repository: cinamoncandy/NUSA
base_branch: main
implementation_branch: ux/full-gap-close-20260902
objective: "Close presentation-only desktop/mobile-web usability gaps in the canonical Paper Trading renderer without changing runtime, trading, risk, credential, or authority semantics."
authority:
  mode: PAPER_ONLY
  liveAuthority: NONE
  productionMutationAllowed: false
  aiAuthority: ZERO_AUTHORITY
scope:
  - "Strengthen account, execution-mode, decision, position, and Paper order visual hierarchy using existing canonical renderer truth."
  - "Improve narrow-screen table readability without fabricating fields or changing data ownership."
  - "Improve touch ergonomics and order-confirmation sheet presentation."
  - "Keep visual status semantics token-driven and accessible."
exclusions:
  - "No connection/freshness runtime logic already covered by PR #1473."
  - "No navigation, theme, chart, timeline, or runtime subscription ownership changes."
  - "No backend, IPC, broker, credential, LIVE, REAL, risk-policy, or financial-authority changes."
  - "No fabricated market, signal, confidence, execution, risk, or PnL values."
safety_boundaries:
  - "Paper Trading remains explicit on every trading-critical surface."
  - "LIVE authority remains NONE."
  - "productionMutationAllowed remains false."
  - "AI remains ZERO_AUTHORITY."
verification:
  required:
    - "pnpm run validate"
    - "pnpm run lint:desktop"
    - "pnpm run test:ui"
    - "git diff --check"
  status: BLOCKED_ENVIRONMENT
  blocker: "Current execution container provides Node 22.16.0 and pnpm 10.9.2, while repository package.json requires Node >=24.0.0 and pnpm >=11.7.0. Repository checkout materialization is also unavailable in this connector environment. No validation PASS is claimed."
  partial_checks:
    - "app-adapter.js syntax check PASS"
    - "stylesheet mount smoke check PASS"
  resume_sequence:
    - "Use Node >=24.0.0 and pnpm >=11.7.0"
    - "Run pnpm run validate"
    - "Run pnpm run lint:desktop"
    - "Run pnpm run test:ui"
    - "Run pnpm run build"
    - "Run git diff --check"
    - "Only after all required local gates pass, create or refresh the single task PR"
completion_criteria:
  - "Canonical desktop hierarchy is materially clearer without runtime duplication."
  - "Primary portfolio tables remain readable at mobile width without mandatory horizontal scrolling for essential fields."
  - "Paper order confirmation remains explicit and touch-safe."
  - "No authority or execution behavior changes."
  - "Required local validation passes before PR creation."
provenance:
  requested_by: owner
  requested_at: "2026-09-02"
  request: "다해"
  latest_request: "4번부터 다 해"
